// gcc ms_signals.c ../utils/*.c ../parser/*.c ../libft/libft.a -lreadline -o signals
#include "../../inc/minishell.h"

void	handle_signal(int n)
{
	write(1, " I WON'T DIE ANYWAY!!\n", 22);
}

int	main(void)
{
	//signal(SIGINT, handle_signal);
	while (1)
	{
		printf("bla-bla-bling in process # %d\n", getpid());
		sleep(1);
	}

	return (0);
}